describe('Organization', () => {
  test('Tests work', () => {
    expect(1).toBe(1);
  });
});
// before all tests
// delete organizations in development database
// create new organizations
// seed database with them

// with some organizations in the database
// getting all returns all organizations
// getting one returns a specific organization
// invalid id returns error

// with correct input
// creating a organization is successful
// deleting a organization is successful
// editing a organization is successful

// with incorrect input
// creating a organization fails with error
// deleting a organization is fails with error
// editing a organization is fails with error

// with organization in use
// deleting fails with an error
