describe('Item', () => {
  test('Tests work', () => {
    expect(1).toBe(1)
  })
})
// before all tests
// delete items in development database
// create new items
// seed database with them

// with some items in the database
// getting all returns all items
// getting one returns a specific item
// invalid id returns error

// with correct input
// creating a item is successful
// deleting a item is successful
// editing a item is successful

// with incorrect input
// creating a item fails with error
// deleting a item is fails with error
// editing a item is fails with error

// with item in use
// deleting fails with an error
