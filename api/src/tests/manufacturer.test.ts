describe('Manufacturer', () => {
  test('Tests work', () => {
    expect(1).toBe(1)
  })
})
// before all tests
// delete manufacturers in development database
// create new manufacturers
// seed database with them

// with some manufacturers in the database
// getting all returns all manufacturers
// getting one returns a specific manufacturer
// invalid id returns error

// with correct input
// creating a manufacturer is successful
// deleting a manufacturer is successful
// editing a manufacturer is successful

// with incorrect input
// creating a manufacturer fails with error
// deleting a manufacturer is fails with error
// editing a manufacturer is fails with error

// with manufacturer in use
// deleting fails with an error
